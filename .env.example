

# =================================================================
# NEXT.JS CONFIGURATION
# =================================================================
#
# SUPABASE CONFIGURATION
#
# Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Supabase anonymous key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Supabase service role key (for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =================================================================
# QSTASH CONFIGURATION (Upstash)
# =================================================================
# QStash is used for scheduling prebooking executions at exact timestamps
# Get these from: https://console.upstash.com/qstash
#
# Cost: ~$1.80/month (3000 messages/month)
# Precision: <100ms (vs 1-10s jitter with cron-job.org)
#
# REQUIRED for prebooking system:
QSTASH_TOKEN=eyJxxx...
QSTASH_CURRENT_SIGNING_KEY=sig_xxx...
QSTASH_NEXT_SIGNING_KEY=sig_xxx...

# PREBOOKING_SECRET: Secret key for HMAC token generation (faster than QStash signature)
# Generate with: openssl rand -hex 32
# Used in HYBRID optimization for fast token validation
PREBOOKING_SECRET=your-random-secret-here-64-chars-hex

# Application URL (for QStash callbacks)
# Local: http://localhost:3000
# Production: Vercel sets this automatically via VERCEL_URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

#
# AIMHARDER API CONFIGURATION
#
# Aimharder login URL
AIMHARDER_LOGIN_URL=https://login.aimharder.com/
# Aimharder fingerprint for API calls (OPTIONAL - fallback only)
# NOTE: By default, each browser generates its own unique 50-character fingerprint
# This is only used as a fallback for server-side operations or testing
# In production, fingerprints are automatically generated per browser and stored in localStorage
AIMHARDER_FINGERPRINT=my0pz7di4kr8nuq718uecu4ev23fbosfp20z1q6smntm42ideb

# =================================================================
# DEPLOYMENT CONFIGURATION
# =================================================================
# Application URL (if not using Vercel)
APP_URL=https://your-app.com

# =================================================================
# SCHEDULED JOBS
# =================================================================
# Prebooking System: QStash schedules individual executions at exact timestamps
#   - No more cron polling every minute
#   - Each prebooking gets its own scheduled execution
#   - Precision: <100ms vs 1-10s jitter
#
# Token Refresh: Still uses GitHub Actions cron (runs every 29 minutes)
#   - CRON_SECRET required for authentication
# PREBOOKING_SECRET: Secret key for HMAC token generation (faster than QStash signature)
CRON_SECRET=your_secure_cron_secret_here
PREBOOKING_SECRET=your-random-secret-here-64-chars-hex


# =================================================================
# RESEND EMAIL CONFIGURATION
# =================================================================
RESEND_API_KEY=xxxxx
RESEND_FROM_EMAIL=xxx@xxx.xxx
ADMIN_EMAIL=xxx@xx.xx