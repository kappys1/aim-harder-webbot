

# =================================================================
# NEXT.JS CONFIGURATION
# =================================================================
#
# SUPABASE CONFIGURATION
#
# Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Supabase anonymous key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Supabase service role key (for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

#
# AIMHARDER API CONFIGURATION
#
# Aimharder login URL
AIMHARDER_LOGIN_URL=https://login.aimharder.com/
# Aimharder fingerprint for API calls (OPTIONAL - fallback only)
# NOTE: By default, each browser generates its own unique 50-character fingerprint
# This is only used as a fallback for server-side operations or testing
# In production, fingerprints are automatically generated per browser and stored in localStorage
AIMHARDER_FINGERPRINT=my0pz7di4kr8nuq718uecu4ev23fbosfp20z1q6smntm42ideb

# =================================================================
# Github Secrets REQUIRED FOR DEPLOYMENT
# =================================================================
# Application URL
APP_URL=https://your-app.com


# =================================================================
# BOTH - Github and Next.js
# =================================================================

# Cron job authentication secret for all cron endpoints
# Used by: prebooking scheduler + token refresh system
# Generate a strong random secret for production
# This must match the CRON_SECRET in GitHub Secrets
# Example: openssl rand -base64 32
CRON_SECRET=your_secure_cron_secret_here

# =================================================================
# GITHUB SECRETS REQUIRED FOR CRON JOBS
# =================================================================
# Add these as GitHub Secrets in your repository settings:
#
# Required:
# - APP_URL: Your production app URL (e.g., https://your-app.vercel.app)
# - CRON_SECRET: Same as CRON_SECRET above (for all cron jobs: prebooking + token refresh)
#
# Optional:
# - DISCORD_WEBHOOK_URL: Discord webhook for critical alerts
#
# Active Cron Jobs:
# 1. Token Refresh: Runs every 29 minutes to keep Aimharder sessions alive
# 2. Prebooking System: Runs at configured times for automatic class bookings